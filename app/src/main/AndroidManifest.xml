<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.simoncherry.arcamera">

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.ARCamActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>
        <!--以下都是测试页面-->
        <activity android:name=".activity.CameraActivity" />
        <activity android:name=".activity.ImageActivity" />
        <activity android:name=".activity.CamAdjustActivity" />
        <activity android:name=".activity.CameraTrackActivity" />
        <activity android:name=".activity.Camera3DActivity" />
        <activity android:name=".activity.CameraRecordActivity" />
        <activity android:name=".activity.Record3DActivity" />
        <activity android:name=".activity.DynamicModelActivity" />
        <activity android:name=".activity.DynamicModel2Activity" />
        <activity android:name=".activity.DynamicModel3Activity" />
    </application>

</manifest>